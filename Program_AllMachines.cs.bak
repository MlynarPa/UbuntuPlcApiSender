using UbuntuPlcApiSender.Services;
using System.Collections.Concurrent;
using System.Text.Json;

Console.WriteLine("=== Ubuntu PLC API Sender - Všechny stroje ===");
Console.WriteLine("Tato aplikace čte data ze všech 4 strojů z PLC");
Console.WriteLine("a odesílá jen vybrané stroje na drevostroj.app API");
Console.WriteLine();

// Konfigurace
var plcIpAddress = "192.168.0.10";
var plcRack = (short)0;
var plcSlot = (short)1;
var apiBaseUrl = "https://drevostroj.app";
var apiKey = "drevostrojapi2024";
var plcReadInterval = 1000; // 1 sekunda pro čtení PLC
var apiSendInterval = 1000; // 1 sekunda pro odesílání API

// Konfigurace strojů pro API (můžete použít jakékoli aliasy)
var machinesWithApi = new[] { "DRS_0001" }; // Můžete použít DRS_0001, DRST_0001, DRS1, DRST1 atd.
var allMachineNames = new[] { "DRS_0001", "DRS_0002", "DRS_0003", "DRS_0004" }; // Používáme DRS názvy

// Thread-safe storage pro data ze všech strojů
var allMachinesData = new ConcurrentDictionary<string, UbuntuPlcApiSender.Models.Machine>();

Console.WriteLine("=== KONFIGURACE ===");
Console.WriteLine($"PLC IP: {plcIpAddress}");
Console.WriteLine($"PLC Rack: {plcRack}, Slot: {plcSlot}");
Console.WriteLine($"API URL: {apiBaseUrl}/api/MachinesApi/{{machineId}}");
Console.WriteLine($"API Key: {apiKey}");
Console.WriteLine($"Interval čtení PLC: {plcReadInterval}ms");
Console.WriteLine($"Interval odesílání API: {apiSendInterval}ms");
Console.WriteLine($"Všechny stroje v PLC: {string.Join(", ", allMachineNames)}");
Console.WriteLine($"Stroje odesílané na API: {string.Join(", ", machinesWithApi)}");
Console.WriteLine("Režim: Čte všechny stroje, odesílá jen vybrané (s podporou aliasů)");
Console.WriteLine($"Podporované aliasy: {string.Join(", ", plcReader.GetSupportedMachineNames())}");
Console.WriteLine();

var plcReader = new PlcReader(plcIpAddress, plcRack, plcSlot);
var apiClient = new ApiClient(apiBaseUrl, apiKey);

Console.WriteLine("Pro ukončení stiskněte Ctrl+C");
Console.WriteLine();
Console.WriteLine("--- Začínají paralelní úkoly: čtení všech strojů a selektivní odesílání API ---");

// CancellationToken pro elegantní ukončení
var cts = new CancellationTokenSource();
Console.CancelKeyPress += (_, e) =>
{
    e.Cancel = true;
    cts.Cancel();
    Console.WriteLine("\n--- Ukončování aplikace... ---");
};

// Task 1: Čtení VŠECH strojů z PLC každou sekundu
var plcReadTask = Task.Run(async () =>
{
    var iteration = 0;
    while (!cts.Token.IsCancellationRequested)
    {
        iteration++;
        
        // Čteme data pomocí aliasů (DRS názvy)
        if (plcReader.TryReadMachinesByAliases(allMachineNames, out var machines, out var errorMessage))
        {
            Console.WriteLine($"[{DateTime.Now:HH:mm:ss}] 📊 PLC #{iteration} - Načteno {machines.Count} strojů:");
            
            foreach (var machine in machines)
            {
                // Uložit data pro všechny stroje (s původním aliasem)
                allMachinesData.AddOrUpdate(machine.Abbreviation, machine, (key, oldValue) => machine);
                
                // Zobrazit data pro každý stroj
                var apiStatus = machinesWithApi.Contains(machine.Abbreviation) ? "→API" : "lokálně";
                Console.WriteLine($"  • {machine.Abbreviation}: {machine.PowerConsumption}W | DI1:{machine.DI1} | DI2:{machine.DI2} | Běží:{machine.IsRunning} ({apiStatus})");
            }
        }
        else
        {
            var message = string.IsNullOrWhiteSpace(errorMessage)
                ? "PLC data se nepodařilo načíst."
                : errorMessage;

            Console.WriteLine($"[{DateTime.Now:HH:mm:ss}] ❌ PLC #{iteration} - {message}");
        }

        try
        {
            await Task.Delay(plcReadInterval, cts.Token);
        }
        catch (OperationCanceledException)
        {
            break;
        }
    }
}, cts.Token);

// Task 2: Odesílání jen VYBRANÝCH strojů na API každou sekundu
var apiSendTask = Task.Run(async () =>
{
    var iteration = 0;
    await Task.Delay(100); // Malé zpoždění aby PLC stihlo načíst první data
    
    while (!cts.Token.IsCancellationRequested)
    {
        iteration++;
        
        var successCount = 0;
        var totalToSend = 0;
        
        foreach (var machineId in machinesWithApi)
        {
            totalToSend++;
            
            if (allMachinesData.TryGetValue(machineId, out var machine))
            {
                var success = await apiClient.SendMachineDataAsync(machineId, machine);
                
                if (success)
                {
                    successCount++;
                }
                else
                {
                    Console.WriteLine($"[{DateTime.Now:HH:mm:ss}] ✗ API #{iteration} - Chyba při odesílání {machineId}");
                }
            }
            else
            {
                Console.WriteLine($"[{DateTime.Now:HH:mm:ss}] ⚠️  API #{iteration} - Žádná data pro {machineId}");
            }
        }
        
        if (totalToSend > 0)
        {
            Console.WriteLine($"[{DateTime.Now:HH:mm:ss}] ✓ API #{iteration} - Odesláno {successCount}/{totalToSend} strojů");
        }

        try
        {
            await Task.Delay(apiSendInterval, cts.Token);
        }
        catch (OperationCanceledException)
        {
            break;
        }
    }
}, cts.Token);

// Task 3: Volitelný monitoring task - zobrazuje přehled každých 10 sekund
var monitorTask = Task.Run(async () =>
{
    await Task.Delay(5000); // Počkat na první data
    
    while (!cts.Token.IsCancellationRequested)
    {
        Console.WriteLine($"\n[{DateTime.Now:HH:mm:ss}] 📈 === PŘEHLED VŠECH STROJŮ ===");
        
        foreach (var machineName in allMachineNames)
        {
            if (allMachinesData.TryGetValue(machineName, out var machine))
            {
                var age = DateTime.UtcNow - machine.Timestamp;
                var apiStatus = machinesWithApi.Contains(machineName) ? "✓ API" : "○ Lokálně";
                Console.WriteLine($"  {machineName}: {machine.PowerConsumption}W | DI1:{machine.DI1} | DI2:{machine.DI2} | Běží:{machine.IsRunning} | {apiStatus} | Stáří:{age.TotalSeconds:F1}s");
            }
            else
            {
                Console.WriteLine($"  {machineName}: ❌ Žádná data");
            }
        }
        Console.WriteLine();
        
        try
        {
            await Task.Delay(10000, cts.Token); // Každých 10 sekund
        }
        catch (OperationCanceledException)
        {
            break;
        }
    }
}, cts.Token);

// Čekání na dokončení všech úkolů
await Task.WhenAll(plcReadTask, apiSendTask, monitorTask);

// Cleanup
apiClient.Dispose();
plcReader.Close();
Console.WriteLine("--- Aplikace ukončena ---");
